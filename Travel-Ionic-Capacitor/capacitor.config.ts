import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.travel',
  appName: 'Travel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
