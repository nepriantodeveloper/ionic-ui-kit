import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.cardekho',
  appName: 'cardekho',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
