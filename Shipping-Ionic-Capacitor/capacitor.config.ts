import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.saska',
  appName: 'Saska',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
