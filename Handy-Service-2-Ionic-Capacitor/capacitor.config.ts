import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.handy.two',
  appName: 'HandyService Two',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
