import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.driver.ui',
  appName: 'Driver UI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
