import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.lottery',
  appName: 'Lottery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
