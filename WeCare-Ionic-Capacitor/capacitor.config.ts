import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.wecare',
  appName: 'WeCare',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
