import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.foodies',
  appName: 'Foodies',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
