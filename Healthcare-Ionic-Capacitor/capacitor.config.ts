import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.helth',
  appName: 'Health App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
