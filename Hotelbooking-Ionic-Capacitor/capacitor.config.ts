import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.hotel.booking',
  appName: 'Hotel Booking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
