import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.event',
  appName: 'Event',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
