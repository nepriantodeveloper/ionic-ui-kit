import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.medicine',
  appName: 'Medicine',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
