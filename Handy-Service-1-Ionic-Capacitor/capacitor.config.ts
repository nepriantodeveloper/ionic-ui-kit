import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.handyman.one',
  appName: 'Handyman One',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
