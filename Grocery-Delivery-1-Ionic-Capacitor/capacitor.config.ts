import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.grocery.one',
  appName: 'Grocery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
