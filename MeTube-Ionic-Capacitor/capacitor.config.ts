import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.metube',
  appName: 'MeTube',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
