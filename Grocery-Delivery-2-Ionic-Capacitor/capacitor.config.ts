import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.grocery.two',
  appName: 'Grocery UI 2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
