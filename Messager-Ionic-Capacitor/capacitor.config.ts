import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.messanger.chat',
  appName: 'Messenger',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
