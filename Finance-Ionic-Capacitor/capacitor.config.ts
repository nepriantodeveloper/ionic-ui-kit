import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.finance',
  appName: 'Finance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
