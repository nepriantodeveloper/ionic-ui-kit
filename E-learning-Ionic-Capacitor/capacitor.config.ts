import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capcitor.elearning',
  appName: 'E-Learning',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
