import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.nomia',
  appName: 'Nomia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
