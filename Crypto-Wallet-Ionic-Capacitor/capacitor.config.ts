import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.crypto',
  appName: 'Crypto Coin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
