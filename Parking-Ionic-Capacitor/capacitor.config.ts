import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.capacitor.parking',
  appName: 'Parking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
